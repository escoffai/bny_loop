# apps/web

React + TypeScript dashboard. Phase 0 is a placeholder workspace so the
monorepo wiring (`pnpm install`, `pnpm -r run lint`) succeeds. Phase 4 lands
the real Vite + Tailwind app — see `BUILDING_WITH_CLAUDE.md`.
