services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bny
      POSTGRES_PASSWORD: bny
      POSTGRES_DB: bny
    ports:
      - "5432:5432"
    volumes:
      - bny_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bny -d bny"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: bny
      MINIO_ROOT_PASSWORD: bnybnybny
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - bny_minio:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  bny_postgres:
  bny_minio:
